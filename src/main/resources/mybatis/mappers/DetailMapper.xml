<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.px.dao.DetailMapper">
    <insert id="receiver" parameterType="detail">
        insert into tb_detail(horseman_id,order_id) values(#{horsemanId},#{orderId})
    </insert>
    <!--多表查询 通过horsemanid去查找id-->
</mapper>